#include "main.h"  // This gives access to the prototype for print_alphabet

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
    print_alphabet();  // Now this is recognized
    return (0);
}
